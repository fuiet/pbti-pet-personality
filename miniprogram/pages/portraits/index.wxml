<view class="page">
  <view class="card">
    <text class="title">云开发写真</text>
    <text class="desc">先把云函数和数据库打通，再接图片生成。</text>
    <button class="primary" bindtap="generate" loading="{{generating}}">调用云函数</button>
    <view wx:if="{{result}}" class="result">
      <text>{{result}}</text>
    </view>
  </view>
</view>
